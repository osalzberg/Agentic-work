# Benchmark KQL Queries from CSV/Excel

This tool allows you to run benchmark batches of natural language prompts by reading them from CSV or Excel files, generating KQL queries, and writing the results back.

See BENCHMARK_WEB.md for web UI instructions.
