source
| extend propertiesTable = properties
| project
    AdditionalFields = todynamic(propertiesTable["AdditionalFields"]),
    ClientVersion = tostring(propertiesTable["ClientVersion"]),
    DeviceId = tostring(propertiesTable["DeviceId"]),
    DeviceName = tostring(propertiesTable["DeviceName"]),
    DeviceObjectId = tostring(propertiesTable["DeviceObjectId"]),
    IsAzureADJoined = tobool(propertiesTable["IsAzureADJoined"]),
    LoggedOnUsers = todynamic(propertiesTable["LoggedOnUsers"]),
    MachineGroup = tostring(propertiesTable["MachineGroup"]),
    OSArchitecture = tostring(propertiesTable["OSArchitecture"]),
    OSBuild = tolong(propertiesTable["OSBuild"]),
    OSPlatform = tostring(propertiesTable["OSPlatform"]),
    OSVersion = tostring(propertiesTable["OSVersion"]),
    PublicIP = tostring(propertiesTable["PublicIP"]),
    RegistryDeviceTag = tostring(propertiesTable["RegistryDeviceTag"]),
    ReportId = tolong(propertiesTable["ReportId"]),
    TimeGenerated = todatetime(propertiesTable["Timestamp"]),
    Timestamp = todatetime(propertiesTable["Timestamp"]),
    TenantId = tostring(tenantId),
    AadDeviceId = tostring(propertiesTable["AadDeviceId"]),
    DeviceCategory = tostring(propertiesTable["DeviceCategory"]),
    DeviceSubtype = tostring(propertiesTable["DeviceSubtype"]),
    DeviceType = tostring(propertiesTable["DeviceType"]),
    JoinType = tostring(propertiesTable["JoinType"]),
    MergedDeviceIds = tostring(propertiesTable["MergedDeviceIds"]),
    MergedToDeviceId = tostring(propertiesTable["MergedToDeviceId"]),
    Model = tostring(propertiesTable["Model"]),
    OnboardingStatus = tostring(propertiesTable["OnboardingStatus"]),
    OSDistribution = tostring(propertiesTable["OSDistribution"]),
    OSVersionInfo = tostring(propertiesTable["OSVersionInfo"]),
    Vendor = tostring(propertiesTable["Vendor"]),
    SensorHealthState = tostring(propertiesTable["SensorHealthState"]),
    IsExcluded = tobool(propertiesTable["IsExcluded"]),
    ExclusionReason = tostring(propertiesTable["ExclusionReason"]),
    AssetValue = tostring(propertiesTable["AssetValue"]),
    ExposureLevel = tostring(propertiesTable["ExposureLevel"]),
    IsInternetFacing = tobool(propertiesTable["IsInternetFacing"]),
    DeviceManualTags = tostring(propertiesTable["DeviceManualTags"]),
    DeviceDynamicTags = tostring(propertiesTable["DeviceDynamicTags"]),
    AzureResourceId = tostring(propertiesTable["AzureResourceId"]),
    AwsResourceName = tostring(propertiesTable["AwsResourceName"]),
    GcpFullResourceName = tostring(propertiesTable["GcpFullResourceName"]),
    AzureVmId = tostring(propertiesTable["AzureVmId"]),
    AzureVmSubscriptionId = tostring(propertiesTable["AzureVmSubscriptionId"]),
    CloudPlatforms = tostring(propertiesTable["CloudPlatforms"]),
    HardwareUuid = tostring(propertiesTable["HardwareUuid"]),
    HostDeviceId = tostring(propertiesTable["HostDeviceId"]),
    IsTransient = tobool(propertiesTable["IsTransient"]),
    MitigationStatus = tostring(propertiesTable["MitigationStatus"]),
    OsBuildRevision = tostring(propertiesTable["OsBuildRevision"])