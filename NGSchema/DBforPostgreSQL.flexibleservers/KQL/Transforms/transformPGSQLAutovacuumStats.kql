source
| project
    TimeGenerated = todatetime(['time']),
    LogicalServerName = LogicalServerName,
    ReplicaRole = ReplicaRole,
    DatabaseName = tostring(properties.DatabaseName),
    SchemaName = tostring(properties.Schemaname),
    LiveRowsCount = tolong(properties.N_live_tup),
    DeadRowsCount = tolong(properties.N_dead_tup),
    SeqentialScansCount = tolong(properties.Seq_scan),
    RowsFromSeqentialScansCount = tolong(properties.Seq_tup_read),
    IndexScansCount = tolong(properties.Idx_scan),
    RowsFromIndexScansCount = tolong(properties.Idx_tup_fetch),
    RowsInsertedCount = tolong(properties.N_tup_ins),
    RowsUpdatedCount = tolong(properties.N_tup_upd),
    RowsDeletedCount = tolong(properties.N_tup_del),
    RowsHotUpdatedCount = tolong(properties.N_tup_hot_upd),
    ModificationsSinceAnalyzeCount = tolong(properties.N_mod_since_analyze),
    VacuumCount = tolong(properties.Vacuum_count),
    AutovacuumCount = tolong(properties.Autovacuum_count),
    AnalyzeCount = tolong(properties.Analyze_count),
    AutoanalyzeCount = tolong(properties.Autoanalyze_count),
    TablesCount = tolong(properties.Tables_counter),
    TablesVacuumedCount = tolong(properties.Tables_vacuumed),
    TablesAutovacuumedCount = tolong(properties.Tables_autovacuumed),
    TablesAnalyzedCount = tolong(properties.Tables_analyzed),
    TablesAutoanalyzedCount = tolong(properties.Tables_autoanalyzed),
    Location = location