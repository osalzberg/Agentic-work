source
| project
    TimeGenerated = todatetime(["time"]),
    OperationName = tostring(operationName),
    Version = tostring(properties.version),
    Region = tostring(properties.region),
    VirtualNetworkId = tostring(properties.vnet_id),
    QueryName = tostring(properties.query_name),
    QueryType = tostring(properties.query_type),
    QueryClass = tostring(properties.query_class),
    ResponseCode = toint(properties.response_code),
    Answer = properties.answer,
    Authority = properties.authority,
    AdditionalRecords = properties.additional,
    SourceIpAddress = tostring(properties.srcipaddr),
    SourcePort = toint(properties.srcport),
    DestinationIpAddress = tostring(properties.dstipaddr),
    DestinationPort = toint(properties.dstport),
    Transport = tostring(properties.transport),
    PrivateResolverEndpointId = tostring(properties.privateresolver_endpoint),
    QueryResponseTime = toint(properties.query_response_time),
    ResolutionPath = tostring(properties.resolution_path),
    ResolverPolicyId = tostring(properties.resolverpolicy_id),
    ResolverPolicyRuleAction = tostring(properties.resolverpolicy_rule_action),
    ResolverPolicyDomainListId = tostring(properties.resolverpolicy_domain_list_id),
    DnsForwardingRulesetId = tostring(properties.dnsforwardingruleset_id),
    DnsForwardingRulesetDomain = tostring(properties.dnsforwardingruleset_domain_list_id)