source
| project
    AADTenantId = tostring(split(resourceId, '/', 2)[0]),
    TimeGenerated = todatetime(["time"]),
    OperationName = operationName,
    OperationVersion = operationVersion,
    ResultType = resultType,
    ResultSignature = resultSignature,
    ResultDescription = resultDescription,
    DurationMs = tolong(durationMs),
    CorrelationId = correlationId,
    InitiatedBy = properties.initiatedBy,
    AdditionalDetails = properties.additionalDetails,
    SourceSystem = "Azure AD",
    Identity = identity,
    Level = Level,
    Location = location,
    LoggedByService = tostring(properties.loggedByService),
    Result = tostring(properties.result),
    ResultReason = tostring(properties.resultReason),
    TargetResources = properties.targetResources,
    ActivityDisplayName = tostring(properties.activityDisplayName),
    ActivityDateTime = todatetime(properties.activityDateTime),
    AADOperationType = tostring(properties.operationType),
    CallerIpAddress = callerIpAddress,
    UserAgent = tostring(properties.userAgent),
    Id = tostring(properties.id)