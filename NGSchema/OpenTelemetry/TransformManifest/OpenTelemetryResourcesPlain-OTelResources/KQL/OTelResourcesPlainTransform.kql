source
| project
    TimeGenerated = todatetime(["time"]),
    Id = tostring(Id),
    ServiceNamespace = tostring(ServiceNamespace),
    ServiceName = tostring(ServiceName),
    ServiceInstanceId = tostring(ServiceInstanceId),
    Attributes = Attributes,
    RoleName = tostring(RoleName)