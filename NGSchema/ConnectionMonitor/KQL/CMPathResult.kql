source
| project
    TimeGenerated = timeGenerated,
    RecordId = recordId,
    TopologyId = topologyId,
    ConnectionMonitorResourceId = connectionMonitorResourceId,
    TestGroupName = testGroupName,
    TestConfigurationName = testConfigurationName,
    SourceType = sourceType,
    SourceResourceId = sourceResourceId,
    SourceAddress = sourceAddress,
    SourceSubnet = sourceSubnet,
    SourceIP = sourceIP,
    SourceName = sourceName,
    SourceAgentId = sourceAgentId,
    DestinationPort = destinationPort,
    DestinationType = destinationType,
    DestinationResourceId = destinationResourceId,
    DestinationAddress = destinationAddress,
    DestinationSubnet = destinationSubnet,
    DestinationIP = destinationIP,
    DestinationName = destinationName,
    DestinationAgentId = destinationAgentId,
    Protocol = protocol,
    ChecksTotal = checksTotal,
    ChecksFailed = checksFailed,
    PathTestResult = pathTestResult,
    PathTestResultCriterion = pathTestResultCriterion,
    ChecksFailedPercentThreshold = checksFailedPercentThreshold,
    RoundTripTimeMsThreshold = roundTripTimeMsThreshold,
    MinRoundTripTimeMs = minRoundTripTimeMs,
    MaxRoundTripTimeMs = maxRoundTripTimeMs,
    AvgRoundTripTimeMs = avgRoundTripTimeMs,
    JitterMs = jitterMs,
    HopAddresses = hopAddresses,
    HopTypes = hopTypes,
    HopLinkTypes = hopLinkTypes,
    HopResourceIds = hopResourceIds,
    Issues = issues,
    Hops = hops,
    AdditionalData = additionalData