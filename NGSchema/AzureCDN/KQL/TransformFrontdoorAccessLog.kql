source
| project 
    TimeGenerated = todatetime(["time"]),
    OperationName = operationName,
    TrackingReference = tostring(properties.trackingReference),
    HttpMethod = tostring(properties.httpMethod),
    HttpVersion = tostring(properties.httpVersion),
    RequestUri = tostring(properties.requestUri),
    HostName = tostring(properties.hostName),
    RequestBytes = tolong(properties.requestBytes),
    ResponseBytes = tolong(properties.responseBytes),
    UserAgent = tostring(properties.userAgent),
    ClientIp = tostring(properties.clientIp),
    SocketIp = tostring(properties.socketIp),
    DurationMs = todouble(properties.timeTaken) * 1000,
    RequestProtocol = tostring(properties.requestProtocol),
    SecurityProtocol = tostring(properties.securityProtocol),
    SecurityCipher = tostring(properties.securityCipher),
    Endpoint = tostring(properties.endpoint),
    HttpStatusCode = toint(properties.httpStatusCode),
    Pop = tostring(properties.pop),
    CacheStatus = tostring(properties.cacheStatus),
    MatchedRulesSetName = tostring(properties.rulesEngineMatchNames),
    RouteName = tostring(properties.routingRuleName),
    ClientPort = toint(properties.clientPort),
    Referrer = tostring(properties.referer),
    TimeToFirstByte = todouble(properties.timeToFirstByte),
    ErrorInfo = tostring(properties.errorInfo),
    OriginUrl = tostring(properties.originUrl),
    OriginIp = tostring(properties.originIp),
    OriginName = tostring(properties.originName)