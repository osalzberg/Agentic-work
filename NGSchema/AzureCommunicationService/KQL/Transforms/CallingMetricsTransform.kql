source
| project
    TimeGenerated = todatetime(["time"]),
    OperationName = operationName,
    OperationVersion = operationVersion,
    Category = category,
    CorrelationId = correlationId,
    TimestampMax = todatetime(properties.timestamp_max),
    TimestampBin = todatetime(properties.timestampBin),
    MetricValueAvg = todouble(properties.metricValue_avg),
    Unit = tostring(properties.unit),
    Goal = tostring(properties.goal),
    FailedLegsDcount = toint(properties.failed_legs_dcount),
    SuccessLegsDcount = toint(properties.success_legs_dcount),
    CallsDcount = toint(properties.calls_dcount),
    LegsDcount = toint(properties.legs_dcount),
    SubCode = tostring(properties.subCode),
    CallType = tostring(properties.callType),
    Platform = tostring(properties.platform),
    ResultType = tostring(properties.resultType),
    DeviceModel = tostring(properties.deviceModel),
    DeviceBrand = tostring(properties.deviceBrand),
    DeviceFamily = tostring(properties.deviceFamily),
    DeviceOsVersionMajor = tostring(properties.deviceOsVersionMinor),
    DeviceOsVersionMinor = tostring(properties.deviceOsVersionMajor),
    DeviceBrowserVersionMinor = tostring(properties.deviceBrowserVersionMinor),
    DeviceBrowserVersionMajor = tostring(properties.deviceBrowserVersionMajor),
    DeviceOsName = tostring(properties.deviceOsName),
    DeviceBrowser = tostring(properties.deviceBrowser),
    SdkVersion = tostring(properties.sdkVersion),
    MetricName = tostring(properties.metricName)