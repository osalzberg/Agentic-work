source
| project
    TimeGenerated = todatetime(["time"]),
    OperationName = operationName,
    OperationVersion = operationVersion,
    Category = category,
    ResultType = resultType,
    ResultSignature = resultSignature,
    ResultDescription = resultDescription,
    DurationMs = toint(durationMs),
    CallerIpAddress = callerIpAddress,
    CorrelationId = correlationId,
    Level = level,
    URI = uri,
    OutgoingMessageLength = toint(properties.outgoingMessageLength),
    IncomingMessageLength = toint(properties.incomingMessageLength),
    DeliveryAttempts = toint(properties.deliveryAttempts),
    PhoneNumber = tostring(properties.phoneNumber),
    NumberType = tostring(properties.numberType),
    SdkType = tostring(properties.sdkType),
    PlatformType = tostring(properties.platformType),
    Method = tostring(properties.method),
    MessageId = tostring(properties.messageId),
    Country = tostring(properties.country)