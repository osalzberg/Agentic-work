source
| project 
    TimeGenerated = todatetime(["time"]),
    OperationName = operationName,
    OperationVersion = operationVersion,
    Category = category,
    CorrelationId = correlationId,
    Identifier              = tostring(properties.identifier),
    CallStartTime           = todatetime(properties.callStartTime),
    CallDuration            = tolong(properties.callDuration),
    CallType                = tostring(properties.callType),
    TeamsThreadId           = tostring(properties.teamsThreadId),
    ParticipantId           = tostring(properties.participantId),
    ParticipantTenantId     = tostring(properties.participantTenantId),
    ParticipantStartTime    = todatetime(properties.participantStartTime),
    ParticipantDuration     = tolong(properties.participantDuration),
    ParticipantEndReason    = tostring(properties.participantEndReason),
    EndpointId              = tostring(properties.endpointId),
    EndpointType            = tostring(properties.endpointType),
    SdkVersion              = tostring(properties.sdkVersion),
    OsVersion               = tostring(properties.osVersion),
    PstnParticipantCallType = tostring(properties.pstnParticipantCallType),
    ParticipantType         = tostring(properties.participantType),
    ParticipantEndSubCode   = tostring(properties.participantEndSubCode),
    ResultCategory          = tostring(properties.resultCategory),
    DiagnosticOptions       = tostring(properties.diagnosticOptions),
    CallDebuggingInfo       = tostring(properties.callDebuggingInfo),
    TPE                     = tobool(properties.ops)