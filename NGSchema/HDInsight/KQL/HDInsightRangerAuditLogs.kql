source
| project
    TimeGenerated = evtTime,
    RepoType = toint(repoType),
    Repo = repo,
    RequestUser = reqUser,
    AccessName = access,
    ClusterResource = resource,
    ClusterResourceType = resType,
    Action = action,
    Result = toint(result),
    Policy = toint(policy),
    Enforcer = enforcer,
    SessionId = sess,
    CliType = cliType,
    CliIpAddress = cliIP,
    RequestData = reqData,
    Id = id,
    SequenceNumber = toint(seq_num),
    EventCount = toint(event_count),
    EventDurationMs = toint(event_dur_ms),
    Tags = tags,
    AdditionalInfo = additional_info,
    LogType = logType,
    ClusterName = clusterName,
    HostName = hostName,
    ClusterType = clusterType,
    OperationName = operationName,
    CorrelationId = correlationId
