source
| project
    TimeGenerated = FluentdIngestTimestamp,
	ReferenceURI = href,
	ClusterName = Alert_cluster_name,
	ComponentName = Alert_component_name,
	DefinitionId = toint(Alert_definition_id),
	DefinitionName = Alert_definition_name,
	AlertFirmness = Alert_firmness,
	HostFQDN = Alert_host_name,
	HostName = hostName,
	AlertID = toint(Alert_id),
	AlertInstance = Alert_instance,
	Label = Alert_label,
	LatestTimestamp = tolong(Alert_latest_timestamp),
	OriginalTimestamp = tolong(Alert_original_timestamp),
	MaintenanceState = Alert_maintenance_state,
	Occurences = toint(Alert_occurrences),
	RepeatTolerance = toint(Alert_repeat_tolerance),
	RepeatToleranceRemaining = toint(Alert_repeat_tolerance_remaining),
	Scope = Alert_scope,
	ServiceName = Alert_service_name,
	State = Alert_state,
	Text = Alert_text,
	ClusterType = clusterType