source
| project
    TimeGenerated = FluentdIngestTimestamp,
    CpuIdle = todouble(cpu_idle),
    CpuNice = todouble(cpu_nice), 
    NumberOfCpu = todouble(cpu_num),
    CpuSystem = todouble(cpu_system),
    CpuUser = todouble(cpu_user),
    CpuIOWait = todouble(cpu_wio),
    DiskFree = todouble(disk_free),
    DiskTotal = todouble(disk_total),
    ReadBytes = todouble(read_bytes),
    ReadCount = todouble(read_count),
    ReadTime = todouble(read_time),
    WriteBytes = todouble(write_bytes),
    WriteCount = todouble(write_count),
    WriteTime = todouble(write_time),
    FifteenMinutLoad = todouble(load_fifteen),
    FiveMinuteLoad = todouble(load_five),
    OneMinuteLoad = todouble(load_one),
    CachedMemory = todouble(mem_cached),
    FreeMemory = todouble(mem_free),
    SharedMemory = todouble(mem_shared),
    TotalMemory = todouble(mem_total),
    FreeSwapMemory = todouble(swap_free),
    BytesIn = todouble(bytes_in),
    BytesOut = todouble(bytes_out),
    PacketsIn = todouble(pkts_in),
    PacketsOut = todouble(pkts_out),
    ProcessesRun = todouble(proc_run),
    TotalProcesses = todouble(proc_total),
    ClusterName = clusterName,
    HostName = hostName,
    ClusterType = clusterType