source
| project
	TimeGenerated = todatetime(['time']),
	OperationName = tostring(operationName),
	Region = tostring(location),
	BackendHost = tostring(properties.backendHost),
	BackendIp = tostring(properties.backendIp),
	BackendPort = toint(properties.backendPort),
	BackendResponseLatency = todouble(properties.backendResponseLatency),
	BackendTimeTaken = toint(properties.backendTimeTaken),
	ClientIp = tostring(properties.clientIp),
	FrontendName = tostring(properties.frontendName),
	FrontendPort = toint(properties.frontendPort),
	HostName = tostring(properties.hostName),
	HttpMethod = tostring(properties.httpMethod),
	HttpStatusCode = toint(properties.httpStatusCode),
	HttpVersion = tostring(properties.httpVersion),
	Referrer = tostring(properties.referrer),
	RequestBodyBytes = tolong(properties.requestBodyBytes),
	RequestHeaderBytes = tolong(properties.requestHeaderBytes),
	RequestUri = tostring(properties.requestUri),
	ResponseBodyBytes = tolong(properties.responseBodyBytes), 
	ResponseHeaderBytes = tolong(properties.responseHeaderBytes),
	TimeTaken = todouble(properties.timeTaken), 
	TlsCipher = tostring(properties.tlsCipher),
	TlsProtocol = tostring(properties.tlsProtocol),
	TrackingId = tostring(properties.trackingId),
	UserAgent = tostring(properties.userAgent)
