source
| project
    TimeGenerated = todatetime(TimeGenerated),
    AzureADTenantId = AzureADTenantId,
    AzureADDeviceId = AzureADDeviceId,
	DeviceName = DeviceName,
	GlobalDeviceId = GlobalDeviceId,
	GroupID = GroupID,
	OSVersion = OSVersion,
	DownloadMode =DownloadMode,
	DownloadModeSrc = DownloadModeSrc,
	ContentDownloadMode = toint(ContentDownloadMode),
	DOStatusDescription = DOStatusDescription,
	Country =Country,
	City = City,
	ISP = ISP,
	PeeringStatus = PeeringStatus,
	ContentType = ContentType,
	TotalTimeForDownload = TotalTimeForDownload,
	TotalTransfers = tolong(TotalTransfers),
	PeerEligibleTransfers = tolong(PeerEligibleTransfers),
	NoPeersCount = tolong(NoPeersCount),
	PeersUnknownCount = tolong(PeersUnknownCount),
	PeersSuccessCount = tolong(PeersSuccessCount),
	PeersCannotConnectCount = tolong(PeersCannotConnectCount),
	BytesFromCDN = tolong(BytesFromCDN),
	BytesFromIntPeers = tolong(BytesFromIntPeers),
	BytesFromPeers = tolong(BytesFromPeers),
	BytesFromGroupPeers = tolong(BytesFromGroupPeers),
	BytesFromCache = tolong(BytesFromCache),
	BWOptPercent7Days = todouble(BWOptPercent7Days),
	BWOptPercent28Days = todouble(BWOptPercent28Days),
	LastCensusSeenTime =todatetime(LastCensusSeenTimeUTC)
	